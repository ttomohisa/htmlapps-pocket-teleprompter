# コンポーネント

`components/` は実行時依存ではなく、再利用用のソーススニペットです。最終成果物は単一HTMLのままです。

## 確認ダイアログ

Pocket Teleprompterでは、原稿の消去など破壊的な操作に `window.confirm()` ではなくアプリ内確認ダイアログを使用します。

キーボードフォーカス、`Esc` キャンセル、背景タップでのキャンセル、フォーカス表示、スマートフォン向けレイアウトを維持します。
